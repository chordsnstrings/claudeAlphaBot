import { StubPage } from "@/components/stub-page";

export default function Page() {
  return (
    <StubPage title="Settings" subtitle="Read-only runtime config: mode, equity, strategies, risk." />
  );
}
