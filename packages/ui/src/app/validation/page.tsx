import { StubPage } from "@/components/stub-page";

export default function Page() {
  return (
    <StubPage
      title="Validation"
      subtitle="Validated artifacts: created_at, composite score, deployment flag, hash."
    />
  );
}
