import { StubPage } from "@/components/stub-page";

export default function Page() {
  return (
    <StubPage
      title="Live Activity"
      subtitle="Streaming feed of bot events — signals, vetoes, trades, regime checks."
    />
  );
}
