import { StubPage } from "@/components/stub-page";

export default function Page() {
  return (
    <StubPage
      title="Commands"
      subtitle="Pause / resume / force-revalidate / close-all / approve-artifact."
    />
  );
}
