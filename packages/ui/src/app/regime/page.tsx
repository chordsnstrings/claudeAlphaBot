import { StubPage } from "@/components/stub-page";

export default function Page() {
  return (
    <StubPage
      title="Regime Monitor"
      subtitle="Per-symbol current regime, 30-day timeline, regime_check_log."
    />
  );
}
