import { StubPage } from "@/components/stub-page";

export default function Page() {
  return (
    <StubPage title="Trade History" subtitle="Full paginated table of trades with filters + CSV export." />
  );
}
